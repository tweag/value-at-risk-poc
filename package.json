{
  "name": "poc_server",
  "engines": {
    "node": "8.9.4"
  },
  "scripts": {
    "build": "cd assets && yarn && yarn build",
    "postinstall": "yarn build",
    "serve": "cd assets && yarn build:watch"
  },
  "devDependencies": {
    "yarn": "^1.3.2"
  }
}
