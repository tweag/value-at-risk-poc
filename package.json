{
  "name": "poc_server",
  "scripts": {
    "build": "cd assets && yarn && yarn build",
    "postinstall": "yarn build",
    "serve": "cd assets && yarn serve"
  },
  "devDependencies": {
    "yarn": "^1.3.2"
  }
}
